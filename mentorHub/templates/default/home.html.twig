{% extends "base.html.twig" %}


{% block title %}Accueil MentorHub{% endblock %}

{% block body %}


{% include'components/_carrousel.html.twig' %}
    <div class="container mt-5">
        <div class="row">
            <h1 class="hero">Bienvenue sur MentorHub</h1>
            <p class="hero">Découvrez notre plateforme de mentorat de carrière </p>
            <div class="col">
            {% if app.user %}
                Bonjour {{ app.user.fullname }} !
            {% endif %}
         </div>
        </div>
    </div>



    <div class="container mt-5">
        <div class="row">
            <h2 class="hero">Nos mentors</h2>
            {% for mentor in mentors %}
                <div class="col">
                    <div class="card">
                        <div class="card-body">
                            <h3 class="card-title">{{ mentor.user.fullName }}</h3>
                            <img src="{{ mentor.image }}" alt="" class="img-fluid">
                            <h4 class="card-title">{{ mentor.slug }}</h4>
                            <p class="card-title">{{ mentor.bio }}</p>
                          
                            <div class="card-body ">
                                <a class="btn btn-primary" href="{{ path('app_profil_mentor', {'id': mentor.id}) }}"
                                   class="card-link">Voir le profil</a>
                                <a class="btn btn-secondary" href="{{ path('app_profil_mentor_sessions', {'id': mentor.id}) }}" class="card-link">Mentorat Session</a>
                            </div>
                        </div>

                    </div>
                </div>

            {% else %}
                <div class="alert alert-info">
                    Pas de Mentor pour le moment.
                </div>
            {% endfor %}

        </div>
    </div>


    <div class="container mt-5">
        <h2 class="display-6">Témoignages de nos utilisateurs</h2>
        <div id="carouselExampleControls" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <div class="card">
                        <div class="card-body">
                            <p class="card-text">"Grâce à MentorHub, j'ai pu trouver un mentor exceptionnel qui m'a beaucoup aidé dans mon développement professionnel. Je recommande vivement cette plateforme à tous ceux qui cherchent à progresser dans leur carrière."</p>
                            <p class="text-end"><em>- Alice, Développeuse Web</em></p>
                        </div>
                    </div>
                </div>
                <!-- Ajoutez d'autres témoignages sous forme de div.carousel-item au besoin -->
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Next</span>
            </button>
        </div>
        <div id="carouselExampleControls" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <div class="card">
                        <div class="card-body">
                            <p class="card-text">"Grâce à MentorHub, j'ai pu trouver un mentor exceptionnel qui m'a beaucoup aidé dans mon développement professionnel. Je recommande vivement cette plateforme à tous ceux qui cherchent à progresser dans leur carrière."</p>
                            <p class="text-end"><em>- Alice, Développeuse Web</em></p>
                        </div>
                    </div>
                </div>
                <!-- Ajoutez d'autres témoignages sous forme de div.carousel-item au besoin -->
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Next</span>
            </button>
        </div>
        <div id="carouselExampleControls" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <div class="card">
                        <div class="card-body">
                            <p class="card-text">"Grâce à MentorHub, j'ai pu trouver un mentor exceptionnel qui m'a beaucoup aidé dans mon développement professionnel. Je recommande vivement cette plateforme à tous ceux qui cherchent à progresser dans leur carrière."</p>
                            <p class="text-end"><em>- Alice, Développeuse Web</em></p>
                        </div>
                    </div>
                </div>
                <!-- Ajoutez d'autres témoignages sous forme de div.carousel-item au besoin -->
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Next</span>
            </button>
        </div>
    </div>

</div>
{% endblock %}