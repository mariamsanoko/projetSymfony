{% extends "base.html.twig" %}


{% block title %}Accueil MentorHub{% endblock %}

{% block body %}
    <div class="container">
    <div class="row">
    <h1  class="display-3 mt-5 mb-3">Nos mentors</h1>

    {% for mentors in mentor %}
        <div class="col">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{ mentors.mentorName }}</h5>

                    <img src="{{ mentors.image }}" alt="" class="img-fluid">
                    <h6 class="card-title">{{ mentors.slug }}</h6>
                     <h5 class="card-title">{{ mentors.bio }}</h5>
                    <hr>
                    <class="btn btn-primary" href="#" class="card-link">Card link</a>

                </div>
            </div>
        </div>

    {% else %}
        <div class="alert alert-info">
            Pas de Mentor pour le moment.
        </div>
    {% endfor %}




{% endblock %}